import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.armaldio.alphadevinette',
  appName: 'AlphaDevinette',
  webDir: 'dist'
};

export default config;
